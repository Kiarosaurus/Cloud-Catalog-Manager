# ---- Frontend React/Vite (Dockerfile inicial DEV - Phase 1) ----
# Phase 4 lo convierte en multi-stage (build Node + serve Nginx).
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev"]
